function checkwavecallback(~, ~, guiNd)
    % CHECKWAVECALLBACK Add the channel to the list of channels to export
    %
    %
    
    %% Call the preview update function.
    updatepreview(guiNd, 'Refresh', true)
end % checkwavecallback
